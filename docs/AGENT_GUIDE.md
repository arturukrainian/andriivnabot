🧭 /docs/AGENT_GUIDE.md

# 🤖 Agent Guide

## 💡 Мета файлу

Цей файл пояснює агенту (AI або людині-розробнику), **як працювати з цим проєктом**, які файли читати для контексту, що дозволено змінювати, як перевіряти результати і як логувати свої дії.  
Він служить **постійною інструкцією**, щоб після кожного перезапуску агент міг швидко відновити стан проєкту.

---

## 📘 1. Порядок завантаження контексту

Після запуску агент повинен **спочатку ознайомитися** з файлами в такому порядку:

1️⃣ `README.md` — бізнес-контекст і загальна архітектура.  
2️⃣ `docs/ARCHITECTURE.md` — технічні компоненти, взаємодії та стек.  
3️⃣ `docs/DEVELOPMENT_PLAN.md` — етапи розробки, що саме робити зараз.  
4️⃣ `docs/AGENT_GUIDE.md` — це інструкція (виконуєш її).

> Якщо проєкт оновлюється, агент має перечитати **ARCHITECTURE.md** і **DEVELOPMENT_PLAN.md**, щоб зрозуміти нові модулі чи зміни у структурі.

---

## ⚙️ 2. Робочі правила

### 🔹 Код

- Пиши **чистий, типізований TypeScript (ESM)**.
- Використовуй **async/await**, жодних callback hell.
- Винось бізнес-логіку в `/src/services/`.
- Не змішуй код Telegram SDK із логікою бази чи аналітики.

### 🔹 Структура папок

- `/src/ingest/` — прийом вебхуків, публікація у чергу.
- `/src/workers/` — воркери, які слухають Pub/Sub і обробляють події.
- `/src/services/` — бізнес-логіка (user, profile, quiz, analytics, payments).
- `/src/db/` — Prisma + репозиторії для БД.
- `/src/utils/` — допоміжні функції (redis, idempotency, pubsub, logger).

### 🔹 Заборонено змінювати

- `prisma/schema.prisma` **без узгодження** (архітектор визначає схему).
- Документацію (`README.md`, `ARCHITECTURE.md`, `DEVELOPMENT_PLAN.md`) — лише за погодженням.
- `.env` — не додавай нові змінні без причини.

---

## 🧩 3. Логування та моніторинг

### Логи

- Використовуй єдиний утиліт `logger` (із `/src/utils/logger.ts`).
- Кожна дія воркера має логувати:  
  [worker:telegram_updates] Received update_id=123456
  [worker:telegram_updates] Processed user_id=42, command=/quiz
  [worker:send_message] Sent message_id=567 status=OK
- Для помилок: `logger.error({ err, context })`.

### Метрики

- Ключові метрики: `queue_depth`, `worker_latency`, `telegram_api_errors`, `p95 end-to-end`.
- Агент може створити базові ендпоїнти `/health` і `/metrics` для моніторингу.

---

## 🔄 4. Перевірка результатів

### Для Ingest (вебхука)

- Повинен відповідати `200 OK` ≤ 150 мс.
- Не обробляє бізнес-логіку.
- Лише кладе подію в Pub/Sub.

### Для Worker

- Повинен обробити подію без помилок.
- Якщо Telegram API повертає `429` або `5xx`, роби retry з backoff.
- Не допускай подвійної обробки (`update_id` → Redis lock або флаг).

### Для Prisma

- Перед пушем схем — `npx prisma validate && npx prisma generate`.
- Міграції — тільки через `npx prisma migrate dev --name ...`.

### Для аналітики (PostHog)

- Перевіряти, що події `app_start`, `webhook_received`, `lesson_start`, `quiz_answer`, `update_dropped_duplicate`, `chat_lock_contention`, `ratelimit_drop` доходять.
- Відправка йде через `utils/analytics.ts` (флаг `ANALYTICS_ENABLED`) — не дублюй клієнти.

---

## 🧠 5. Робочий цикл агента

1️⃣ Прочитай `DEVELOPMENT_PLAN.md` і знайди активний етап.  
2️⃣ Виконай усі завдання цього етапу.  
3️⃣ Після завершення — додай коментар у кінець `DEVELOPMENT_PLAN.md`:  
✅ [дата] Етап X виконано: короткий опис змін.
4️⃣ Перевір логіку вручну або через тестові update-и.
5️⃣ Лише після валідації переходь до наступного етапу.

---

## 🧰 6. Інструменти та стилі

### Code style

- **Prettier** + **ESLint (airbnb-typescript)**.
- Назви змінних у стилі camelCase.
- Функції мають бути короткі, не більше 30 рядків.
- Використовуй JSDoc-коментарі над складними функціями.

### Документація коду

- Кожен воркер → файл `README.md` у своїй папці (`/src/workers/README.md`), де описано, що він робить.
- Не залишай “магічні числа” або незрозумілі константи.

---

## 🔐 7. Безпека

- Використовуй `.env` для всіх секретів (BOT_TOKEN, DB_URL, POSTHOG_API_KEY).
- Перевіряй `X-Telegram-Bot-Api-Secret-Token` у вебхуку.
- Ніколи не логуйте токени або приватні дані користувачів.
- Валідуй усі вхідні дані (особливо callback_data).

---

## 🔎 8. Як тестувати локально

1️⃣ Запусти локально Postgres (Docker або Neon).  
2️⃣ Налаштуй Redis (Upstash / локальний Docker).  
3️⃣ Вкажи свій webhook через ngrok:  
ngrok http 8080
setWebhook url=https://<ngrok>.io/webhook
4️⃣ Відправ тестові повідомлення.
5️⃣ Перевір логи + PostHog події.

---

## 🧩 9. Якщо агент не впевнений

- Якщо неясно, як реалізувати функцію — спершу уточни її контекст у `ARCHITECTURE.md`.
- Якщо немає інформації — створи коментар `// TODO: clarify with team` у коді.
- Якщо помітив потенційний ризик — логни в `/src/utils/warnings.log`.

---

## ✅ 10. Підсумок

**Мета агента** — будувати код згідно архітектури, не відходячи від плану,  
зберігати логіку чистою, без дублювання,  
і завжди пам’ятати:

> Webhook → Черга → Воркер → Telegram → Аналітика.

---

_Цей файл не редагується без згоди архітектора проєкту._

---


## 🔒 11. Правила ведення `PROGRESS_LOG.md` (оновлено)

Цей розділ є обов’язковим до виконання. Будь-яке порушення вимагає негайного виправлення, оновлення журналу та пояснення причин.

---

### ❌ 11.1 Заборона фіктивних записів

Агент не має права додавати у `docs/PROGRESS_LOG.md` записи, що описують виконане завдання, якщо фактично:

- відповідний файл не існує у репозиторії;
- код не проходить `npm run build` або `npm run lint`;
- логіка не була протестована локально або через симуляцію;
- зміна не була підтверджена людиною (Арчі), якщо це потрібно.

**Заборонені формулювання:**

- “реалізовано X”
- “створено файл Y”
- “додано Z”
- “endpoint готовий”
- будь-який запис, що подає майбутню роботу як факт

---

### 📝 11.2 Як правильно логувати ПЛАН

Якщо агент хоче зафіксувати майбутню задачу або структуру наступного кроку — використовується формат **ПЛАН**:

📝 [дата] — ПЛАН: <назва>
Опис: що буде реалізовано на наступному етапі.
Result: код ще НЕ створено.
Перевірено: ❌
Коментар: буде виконано згідно з DEVELOPMENT_PLAN.md.

ПЛАН — це не виконана робота, а лише опис майбутньої реалізації.

---

### ✔️ 11.3 Коли дозволено писати “Реалізовано”

Запис у стилі:

⚙️ [дата] — Реалізовано <назва>

дозволено **лише якщо всі умови виконані**:

- файл або код реально існує в репозиторії;
- build і lint проходять без помилок;
- логіку протестовано локально (curl, Postman, mock update);
- відповідна секція Active Task оновлена;
- зміна узгоджена з DEVELOPMENT_PLAN.md;
- людина (Арчі) може підтвердити коректність.

---

### 🛠️ 11.4 Наслідки порушення

Якщо агент зробив фіктивний запис:

1. **Негайно виправити PROGRESS_LOG.md**  
   - змінити помилковий запис → формат ПЛАН  
   - оновити Active Task, якщо там є неправильні дані  

2. **Створити запис про виправлення:**  
   - що було виправлено;  
   - чому;  
   - який тепер правильний стан.  

3. **Перепросити за порушення інструкції** і зобов’язатися дотримуватися правил надалі.

---

### 🔍 11.5 Очікувана поведінка перед логуванням

Перед додаванням запису агент повинен:

- перевірити наявність файлів у репозиторії;
- звірити зміни з DEVELOPMENT_PLAN.md;
- перевірити build / lint (якщо застосовно);
- виконати локальний тест (ендпоінт, модуль чи логіка);
- якщо хоч щось під питанням — логувати як **ПЛАН**, а не “реалізовано”.

---
